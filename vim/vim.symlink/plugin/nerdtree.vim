"===============================================================================
" NERDTree settings | Because sometimes you just need a file tree
"===============================================================================

" better nerdtree splits
set splitbelow
set splitright

" NERDTree is open when starting vim in a directory. This will close it
" after opening your first file.
let g:NERDTreeQuitOnOpen = 0
